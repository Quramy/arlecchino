# configuration:
#   base_uri: https://github.com
# scenario:
#   - description: Login
#     steps:
#       - goto: /login
#       - screenshot
#       - find:
#           query: input[name='login']
#           action:
#             - input: Quramy
#       - find:
#           query: input[name='password']
#           action:
#             - input: "{{ $env.GH_PASSWORD }}"
#       - screenshot
#       - find:
#           query: input[name='commit']
#           action: click
#       - wait_for_navigation
#       - screenshot
description: Search TypeScript via Google
configuration:
  base_uri: https://google.co.jp
steps:
  - goto: /search
  - find:
      query: input[name='q']
      action:
        input: TypeScript
  - sleep: 300
  - screenshot
  - find:
      query: input[type='submit']
      action: click
  - wait_for_navigation
  - find:
      query: .srg a
      action: click
  - wait_for_navigation
  - screenshot
